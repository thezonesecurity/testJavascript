//operaciones asincronas => es aquella que no espera al proceso de operaciones intencivas duera de su hilo de procesamiento

console.log("w");
const mypromise = new Promise({resolve, reject} => {

});

'2' === 2 => false;  (a==b) && typeof a  == typeof b
//ajax => 

//fecht => ace peticiones a una api y consume sus recursos

// closhe es cuando una funcion hijo tiene acceso a las variables de la funcion padre

get 
post
//async await se usa en funciones de tipo promesas

lo usa el kernel de linux
fue escrito por linus tolvals
usado en proyectos grandes como gnome 

similares a git => mercurial, bazar, cvs, rcs, "bitkeeper"

pull extrae el codigo a nuestro pc
push sube el codigo a un repositorio